name:  Telegram new-release notification

on:
  release:
    types: [released]

jobs:
  telegram-msg:
    runs-on: ubuntu-latest

    steps:
      - name: Telegram Notify
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          format: markdown
          message: |
            [mintbase.js](https://github.com/Mintbase/mintbase-js/pull/) 🤘 New Release: [${{ github.event.release.tag_name}}](${{ github.event.release.html_url }})
